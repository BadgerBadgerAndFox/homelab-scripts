# LXC resource tuning — 2025-06-06

Changes applied live via `pct set` based on health audit findings.
No restarts required; all changes take effect immediately for memory/swap.

## Changes applied

| CT  | Name       | Change                        | Reason                                                         |
|-----|------------|-------------------------------|----------------------------------------------------------------|
| 121 | whisparr   | swap 1024 → 0                 | 234 MB swap active despite only 44% RAM used; fragmentation    |
| 128 | byparr     | memory 1024 → 2048            | Regularly hitting ceiling under load; 122 MB swap observed     |
| 130 | authentik  | swap 2048 → 0                 | 350 MB swap at only 25% RAM; idle workers being paged out      |

## Already correctly configured (no change needed)

| CT  | Name           | Notes                                                          |
|-----|----------------|----------------------------------------------------------------|
| 103 | tautulli       | memory already 1024, rootfs already 8G                        |
| 112 | cloudflared    | rootfs already 4G                                             |
| 101 | tailscale      | rootfs already 4G                                             |
| 119 | huntarr        | memory already 2048                                           |
| 121 | whisparr       | memory already 2048                                           |
| 122 | traefik        | memory already 256 (correctly lean for a proxy)               |

## Remaining watch items (not changed, monitor)

- **ollama (117)**: disk at 63% of 74 GB — grows as models are pulled; no action yet
- **NAS share**: 72% (10/14 TB) — review media retention before hitting 80%
- **byparr (128)**: disk at 68% of 10 GB — monitor; extend if growth continues
- **Stopped CTs 104/106/110**: configarr, notifiarr, recyclarr — intentionally stopped;
  consider reducing rootfs allocation if permanently unused

## Commands used

```bash
pct set 121 --swap 0
pct set 128 --memory 2048
pct set 130 --swap 0
```
